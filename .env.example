# Gemini API Key
GEMINI_API_KEY=

# Firebase Admin SDK configuration (optional if running in Google Cloud)
# GOOGLE_APPLICATION_CREDENTIALS=path/to/service-account.json

# Node Environment
NODE_ENV=development
PORT=3000
